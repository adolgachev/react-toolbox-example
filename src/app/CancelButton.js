import React from 'react';
import { Button } from 'react-toolbox/lib/button/Button';
import theme from './CancelButton.scss';

const CancelButton = (props) => <Button {...props} theme={theme} />;
export default CancelButton;
